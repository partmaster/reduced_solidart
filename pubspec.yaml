name: reduced_solidart
version: 0.3.3
description: Implementation of the state management API 'reduced' on top of the state management framework 'Solidart'.
homepage: https://github.com/partmaster/reduced_solidart
repository: https://github.com/partmaster/reduced_solidart.git
publish_to: none

environment:
  sdk: '>=2.19.3 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_solidart: ^0.3.0
  reduced: 0.3.2
#  reduced: 
#    git:
#      url: https://github.com/partmaster/reduced.git
#      ref: v0.3.0
  solidart: ^0.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
